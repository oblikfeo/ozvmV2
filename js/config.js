/*
  Единая точка настройки розничной части.

  Здесь лежит всё, что зависит от окружения: адрес API основного проекта
  и внешние ссылки на ozvm.ru. Когда появится каталог, страницы будут брать
  базовый адрес API отсюда, а не зашивать его у себя.

  Файл подключается первым на каждой странице, до остальных скриптов.
*/

window.OZVM = window.OZVM || {};

window.OZVM.config = {
  /* Laravel основного проекта. CORS там открыт (Access-Control-Allow-Origin: *),
     поэтому поддомен может ходить в API из браузера без правок на бэкенде. */
  apiBaseUrl: "https://api.ozvm.ru",

  /* Основной сайт: оптовый магазин. */
  mainSiteUrl: "https://ozvm.ru",

  /* Личный кабинет партнёра и регистрация — пока обе ведут в основной проект.
     Регистрация там живёт на корне: это переключатель на экране авторизации,
     отдельного адреса у неё нет. */
  lkUrl: "https://ozvm.ru/lk",
  registrationUrl: "https://ozvm.ru/",

  /* Ключ, под которым api.js держит токен в localStorage.
     Сейчас токен один — администраторский. Когда появится вход покупателя,
     ключи разведём: у клиента и у админа разные права. */
  tokenKey: "RETAIL_ADMIN_TOKEN",
};
